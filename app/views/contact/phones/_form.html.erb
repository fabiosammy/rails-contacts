<%= form_with(model: contact_phone, local: true) do |form| %>
  <% if contact_phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact_phone.errors.count, "error") %> prohibited this contact_phone from being saved:</h2>

      <ul>
      <% contact_phone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
